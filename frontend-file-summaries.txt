{
  "frontend/src/index.css": "Global styles with OKLCH dark navy/gold/sky-blue color system, Rajdhani + Inter fonts, and utility classes — fixed to avoid opacity modifier syntax inside `@apply` directives which caused the PostCSS build error.",
  "frontend/tailwind.config.js": "Extended Tailwind config with custom navy, gold, sky, success color tokens, Rajdhani/Inter font families, and gold/navy shadow utilities.",
  "frontend/src/hooks/useQueries.ts": "React Query hooks for all backend operations — now includes all previously existing hooks plus the four newly added ones: `useStudentLogout` (mutation calling `actor.studentLogout`), `useGetStudentProfile` (query by token), `useUpdateStudentProfilePhoto` (mutation with cache invalidation), and `useGetStudents` (admin query by token), fixing all TypeScript import errors in `StudentDashboard.tsx` and `AdminStudents.tsx`.",
  "frontend/src/App.tsx": "Root app component with path-based routing that tracks both `currentPage` and `currentPath` state, passes `currentPath` to `AdminDashboard`, wraps everything in `LanguageProvider`, and renders the appropriate page or public home sections.",
  "frontend/src/components/Header.tsx": "Sticky dark-navy header with navigation links, Student Login button, and a prominent EN|HI language toggle button that reads from and updates LanguageContext — all nav labels use the t() translation function.",
  "frontend/src/components/Hero.tsx": "Full-width hero section with translated headline, subheadline, Start Free Test CTA, and animated stats using the useTranslation hook.",
  "frontend/src/components/ExamCategories.tsx": "Exam categories section with translated section title and category pill buttons using useTranslation.",
  "frontend/src/components/FreeTests.tsx": "Free tests section redesigned with improved card layout — dark navy (#0A1628) background cards with a colored top accent bar per category, a pill-shaped category label, a vivid green solid FREE badge (clearly visible), metadata row with FileQuestion/Clock icons, a thin divider, and a full-width outlined Start Test button with category-colored hover states. Preserves all TestTakingModal integration and loading/error/empty states.",
  "frontend/src/components/TestTakingModal.tsx": "Full-screen test-taking modal that reads the current language from LanguageContext and renders questionHi/optionsHi/explanationHi fields in Hindi medium with English fallback, plus all static UI labels translated via useTranslation.",
  "frontend/src/components/PaidTestSeries.tsx": "Premium plans section that fetches real paid tests via `useGetTests`, displays plan cards and available paid tests grid — bank payment details block has been removed entirely.",
  "frontend/src/components/WhyChooseUs.tsx": "Four-card feature highlights section with per-card vivid accent colors (sky, gold, green, orange) using inline styles for gradients and borders, fully restored colorful theme.",
  "frontend/src/components/TopRankers.tsx": "Leaderboard section with translated section heading, table column labels, and status messages using useTranslation.",
  "frontend/src/components/AITestGenerator.tsx": "AI test generator section that now reads the admin session token via `getSessionToken()` and passes it to `generateQuestions({ token, topic, difficulty })`, with inline error handling for missing/invalid admin sessions.",
  "frontend/src/components/Contact.tsx": "Contact form section with all labels, placeholders, and status messages translated via useTranslation.",
  "frontend/src/components/Footer.tsx": "Dark navy footer with translated policy links, copyright text, and caffeine.ai attribution using useTranslation.",
  "frontend/src/hooks/useAuth.ts": "Custom hook and utility functions for managing the admin session token in localStorage — exports `getSessionToken`, `setSessionToken`, `clearSessionToken`, and `useSessionToken` hook that syncs with storage events.",
  "frontend/src/pages/AdminLogin.tsx": "Standalone admin login page with a centered card containing username/password fields (with show/hide toggle), inline error display for invalid credentials, and a login form that calls the backend `login()` mutation and stores the session token on success.",
  "frontend/src/pages/AdminDashboard.tsx": "Admin dashboard layout that guards access via session token, renders AdminSidebar, and routes to the appropriate panel — now includes the `/admin/paid-tests` route mapping to the new AdminPaidTests component.",
  "frontend/src/components/AdminSidebar.tsx": "Admin sidebar with navigation links including a new \"Paid Tests\" entry (CreditCard icon) positioned after \"Tests\", plus all existing nav items, logout, and back-to-site buttons.",
  "frontend/src/pages/AdminTests.tsx": "Tests management panel — updated the \"Category\" column header to \"Exam Category\" and renders each test's category as a colored badge using `getExamCategoryLabel` and `EXAM_CATEGORY_COLORS` from the new utility module.",
  "frontend/src/pages/AdminRankers.tsx": "Rankers management panel showing the leaderboard in a table with rank, name, category, and score columns, plus Add Ranker button (opens `RankerFormModal`) and Delete action per row with gold/silver/bronze rank styling.",
  "frontend/src/pages/AdminContacts.tsx": "Read-only admin panel for contact form submissions — removed the broken `useGetContactSubmissions` hook call (which called a non-existent backend method causing TypeScript errors), replaced with a local `ContactSubmission` interface and an empty array, and shows an informational banner explaining the feature is pending backend API support.",
  "frontend/src/components/TestFormModal.tsx": "Admin modal for adding/editing tests — replaced the old string-based \"Category\" dropdown with an \"Exam Category\" select using the `ExamCategory` enum and `EXAM_CATEGORY_OPTIONS` from the new utility, with proper state typing and form submission using the enum value.",
  "frontend/src/components/RankerFormModal.tsx": "Modal dialog for adding a new ranker with student name, exam category dropdown, and score (0–100) inputs, calling the `addRanker` mutation with the session token on save.",
  "frontend/src/hooks/useStudentAuth.ts": "New student authentication utility hook that manages a separate student session token and username in localStorage, with `getStudentToken`, `setStudentToken`, `clearStudentToken`, `getStudentUsername`, `setStudentUsername`, and a `useStudentAuthToken` React hook that syncs state across storage events.",
  "frontend/src/pages/StudentLogin.tsx": "Student login/register page with a tabbed interface — Login tab has Mobile Number and Password fields (with show/hide toggle), Register tab has Mobile Number, Password, and Confirm Password fields with inline mismatch validation; both tabs call the new `useStudentLogin`/`useStudentRegister` hooks, store the session token on success, and navigate to the student dashboard.",
  "frontend/src/pages/StudentDashboard.tsx": "Protected student dashboard at `/student/dashboard` that validates the session token on mount (redirecting to `/student/login` if invalid), displays a welcome message with the student's username, quick-action cards for Free Tests/Top Rankers/Paid Tests, and a Logout button that clears the token and redirects.",
  "frontend/src/contexts/LanguageContext.tsx": "New React context that stores the current language medium ('en' or 'hi'), reads from and writes to localStorage under the key 'languageMedium', and exposes { language, setLanguage } to all child components via LanguageProvider.",
  "frontend/src/utils/translations.ts": "Comprehensive bilingual (EN/HI) translations utility — extended with new exam category label keys (`examCategoryLabel`, `examCategoryUpsc`, `examCategoryBpsc`, `examCategorySsc`, `examCategoryRailway`, `examCategoryBanking`, `examCategoryStateExams`, `examCategoryGeneral`) in both English and Hindi.",
  "frontend/src/hooks/useTranslation.ts": "Custom hook that consumes LanguageContext and returns a t(key) function that looks up translation keys for the current language with English fallback.",
  "frontend/src/utils/examCategories.ts": "New utility module that exports `ExamCategory` label maps (English and Hindi), dropdown options array, accent color definitions per category, and a `getExamCategoryLabel` helper function with a 'General' fallback.",
  "frontend/src/pages/AdminPaidTests.tsx": "Admin page for managing paid tests — replaced the old TestFormModal with the new PaidTestWizard component, opening it in create mode (Add button) or edit mode (Edit row button) with pre-populated data, while keeping the existing table display and delete confirmation dialog unchanged.",
  "frontend/src/components/QuestionManager.tsx": "New reusable component for managing a list of questions in the wizard's Step 2 — renders each question as a collapsible card with numbered badge, expand/collapse toggle, up/down reorder arrows, inline delete confirmation, and full bilingual (EN/HI) question form with options and correct answer selection.",
  "frontend/src/components/PaidTestWizard.tsx": "Full-screen multi-step wizard overlay for creating and editing paid tests — implements a 4-step flow (Basic Info → Questions → Settings → Review & Publish) with a visual stepper/progress bar, per-step validation, preserved state across navigation, and calls useAddTest or useUpdateTest on final submit with a success toast."
}